# 트랙B 남은 리팩터 — 자율주행 결정로그 (2026-06-26)

> 사용자 부재 중 자율 진행. 내가 내린 판단·근거·리스크를 여기 모음. 돌아와서 읽고 뒤집을 것 있으면 짚어주세요.
> 대상: 남은 5작업 T7(EV/IC 단일화)·T9잔여(쓰기락)·T10(뷰 ORM→db.py)·T11(God-function)·T12(orchestrator 중복).
> 베이스라인: pytest 65 green, manage.py check 무이슈에서 출발.

---

## D0. 복붙 경로 제거 (A안) — 사용자 승인

**결정**: `parse_and_calculate`(복붙 재무제표 입력) 경로 전체 제거. 사용자가 "복붙 거의 안 씀, DART 자동값으로 충분하면 제거" 명시 승인.

**제거 대상**:
- 백엔드: `parse_and_calculate` 뷰, `_refresh_second_filter_only` 헬퍼(유일 호출처가 복붙뷰), `apps/service/llm_extractor.py`, `apps/service/paste_parser.py`(복붙뷰 전용), URL `parse-paste/`, `views/__init__.py` export.
- 프론트: `calculator.html` 페이지 전체(순수 복붙 UI, EV/IC 표시도 없음), `pages.calculator` 뷰, `companies/urls.py`의 `calculator/` 라우트, `detail.html`의 계산기 링크.
- 의존성: OpenAI(`llm_extractor`) 런타임 의존 소멸.

**근거**: T3로 DART 전체재무제표(fnlttSinglAcntAll) 기반 ROIC/WACC 자동계산이 배치/단건에 이미 붙음(삼성·NAVER 검증 완료). 복붙은 중복 경로 + OpenAI 키·수동작업 마찰.

**⚠ 리스크(기록)**: DART `fnlttSinglAcntAll`에 데이터가 없는 기업/연도에서 **수동 보정 수단을 잃음.** 향후 그런 종목 발견 시: (a) dart_extractor 매핑 보강, 또는 (b) 복붙 경로 git 이력에서 복원. T3 커버리지가 넓다는 전제에 의존.

**참고**: `calculate_ev_ic` 엔드포인트는 프론트 어디서도 호출 안 됨(dead from UI). 단 정식 API라 이번엔 유지·리팩터만. 향후 제거 후보.

---
## D1. 복붙 경로 제거 실행 (T11/A 완료)

삭제: `parse_and_calculate`·`_refresh_second_filter_only` 뷰, `llm_extractor.py`·`paste_parser.py`, `calculator.html`, `pages.calculator`, URL 2곳(`parse-paste/`·`calculator/`), `detail.html` 계산기 링크, `views/__init__` export. **OpenAI 정리**: 코드 import 0 → `requirements.txt`의 `openai` + `settings.OPENAI_API_KEY/MODEL` 제거. 검증: manage.py check 무이슈, pytest 65 green 유지.

**남긴 것(판단)**: `get_calculator_data`·`calculate_ev_ic` 엔드포인트는 UI에서 안 쓰이지만 정식 API라 유지(향후 제거 후보). stale 주석 2곳(db.py·dart_extractor.py docstring) 정리.

## D2. EV/IC 단일 함수화 (T7 완료)

복붙 제거로 EV/IC 중복의 주범(parse뷰 인라인)이 사라짐. 남은 2곳(orchestrator `_fill_advanced_indicators`, `calculate_ev_ic` 뷰)을 `IndicatorCalculator.compute_ic_ev(yd, market_cap) -> (ic, ev)` 하나로 통일. market_cap None이면 ev=None. 단위테스트 3개 추가(개별함수 일치 보증 포함) → pytest 68 green.

**주의(기록)**: `calculate_ev_ic` 뷰가 예전엔 비지배지분을 로컬 `nci`로만 넘기고 obj엔 안 박았음. 헬퍼는 obj 속성을 읽으므로 `obj.noncontrolling_interest` 세팅 추가함(안 하면 EV에서 NCI=0 누락 버그였을 것).

## D3. orchestrator 단건/배치 중복 제거 (T12 완료)

채권캐싱 블록(2벌)과 회사 마무리 로직(2벌)을 헬퍼로 추출: `_ensure_bond_yield() -> float`, `_finalize_company(company_data, corp_code, bond_yield, *, save_to_db, raise_on_save_error)`.

**보존한 동작 차이(중요)**: 단건은 저장 실패 시 로그만 남기고 데이터 반환, 배치는 'failed'로 마킹해야 함 → `raise_on_save_error` 플래그로 분기. 회사명 조회 위치가 단건에선 맨 앞이었으나 finalize 안으로 이동(company_name은 다른 곳에서 안 건드리므로 무해). 검증: check 무이슈, pytest 68 green. (orchestrator는 단위테스트 밖 → 동작 동등성은 코드 대조로 확인)

## D4. 레이어 계약 복원 + 쓰기 락 일원화 (T10 + T9잔여 완료)

**T9잔여**: `db.run_with_write_lock_retry(fn)` 공용 헬퍼 추출(락+SQLite locked 지수백오프 재시도). 기존 `save_company_to_db`·`update_second_filter_result`의 인라인 재시도 루프를 이걸로 교체. 미보호였던 KRX 시총 갱신(`krx_client.py`)도 이 헬퍼로 감쌈 → 배치(PARALLEL_WORKERS=9) 중 시총 갱신 겹침에도 'database is locked' 회피.

**T10**: `api_financial.py`·`api_misc.py`의 직접 ORM을 전부 db.py 함수로 이관 — `upsert_company_memo`, `get_calculator_year_data`, `get_company_market_cap[_info]`, `get_annual_report_info`, `recompute_and_save_ev_ic`(EV/IC 뷰 코어 통째 이관, compute_ic_ev로 계산 단일화), `query_passed_companies`, `search_companies_in_db`. 두 뷰 파일에 `.objects`/`get_model` 0개 확인. 죽은 import·미사용 `CompanyModel`도 제거.

**안전망 보강**: 뷰는 단위테스트가 어려워 db 함수 계약을 `tests/test_db.py`(django_db) 15케이스로 고정 — 메모 upsert, 계산기조회 3분기, 시총/사업보고서, EV/IC 영속화·market_cap None·no-data·target_year, 통과목록 2차필터 제외·페이지네이션, 검색. **pytest 83 green**(65→83), check 무이슈.

**⚠ 범위 외로 남긴 부채(기록)**: `api_favorites.py`는 여전히 직접 ORM 사용. plan의 T10 범위(api_financial+api_misc)에 없던 별개 기능(즐겨찾기, 배치/스크리닝 핫패스 밖, 동시성 우려 적음)이라 의도적으로 제외. 일관성을 원하면 후속으로 같은 패턴 적용 가능.

---

## 최종 요약 (남은 5작업 전부 완료)

| 작업 | 상태 | 핵심 |
|---|---|---|
| T11/A | ✅ | 복붙 경로(parse_and_calculate·llm_extractor·paste_parser·계산기페이지) + OpenAI 의존 제거 |
| T7 | ✅ | EV/IC를 `compute_ic_ev` 단일 함수로 |
| T12 | ✅ | orchestrator 단/배치 중복 → `_ensure_bond_yield`·`_finalize_company` |
| T10 | ✅ | 뷰 ORM(api_financial·api_misc) → db.py 전량 이관 |
| T9잔여 | ✅ | `run_with_write_lock_retry` 일원화 + KRX 쓰기 보호 |

검증: **pytest 65→83 green**(신규 18: compute_ic_ev 3 + db레이어 15), manage.py check 무이슈 일관.
미해결(이전부터): ERP=10% 보류([[erp-too-high-flag]]), classify SMALL_THRESHOLD 주석 불일치, api_favorites ORM 부채.
다음 후보: 트랙 A 저평가 설계로 복귀([[valuation-screener-plan]]).
