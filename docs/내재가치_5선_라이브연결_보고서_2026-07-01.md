# 내재가치 5선 라이브 연결 보고서 (2026-07-01)

## 한 줄 결론
계산만 돼 있고 죽어 있던 내재가치 5선(g·Altman Z''·Zmijewski·EV/IC·ROIC−WACC)을
상세화면·목록화면 API에 라이브 연결 완료. 그 과정에서 roic 미계산을 0으로 오인하던
근본 결함을 None으로 통일해 차단. g의 신뢰성은 배당 데이터 수집 누락(99.6%)으로
제약되며 별도 후속 과제로 식별.

## 진행 단계 (4 phase, 각 phase 커밋)

### Phase 0 — 모델·마이그레이션
- YearlyFinancialData에 연도별 5선 컬럼 추가: sustainable_growth·altman_z·altman_z_class·
  zmijewski·zmijewski_flag (마이그레이션 0021). 회사단위 플래그(FCF음수·무차입의심)는
  윈도우 판정이라 저장 제외.
- 모델·객체·load·save 4곳 동기화.

### Phase 1 — 계산·저장 파이프라인 + roic None 통일(방향 B)
- fill_valuation_indicators 헬퍼 신설(6개 calculate_*/classify/flag 조합). orchestrator가
  전 연도에 호출(continue 분기 포함 → 재수집 시 5선 소실 방지). backfill command로
  기존 저장 입력에서 5선 백필(DART 재수집 없음).
- **근본 결함 차단**: roic/wacc/fcf 미계산 표현을 0.0→None 통일(객체 기본값 + 마이그레이션
  0022로 기존 14,879행 정리). roic=0.0(=100% 미계산, 진짜 0 손실 0 확인)을 calculate_g가
  유효로 보아 가짜 g=0.0을 9,251행에 박제하던 문제를 차단.
- 백필 실행: g 445행(가짜0 0건), Z'' 14,551행, Zmijewski 14,765행.

### Phase 2 — 상세화면
- get_financial_data 직렬화에 연도별 5선 + 회사단위 FCF 음수 경보 추가.
- detail.html 테이블에 성장률 g·Altman Z''·Zmijewski 컬럼 + 등급/경보 색상.
- 실사용 검증: 00125664 2024 g=2.45%·Z''=3.73(safe)·Zmijewski=23.6%.

### Phase 3 — 목록 랭킹
- rank_passed_companies(): 각 회사 'roic 있는 최근연도' 대표 스냅샷 3축(quality=roic−wacc·
  price=EV/IC·growth=g) → rank_companies 순위합산 → corp_code 맵. N+1 회피.
- query_passed_companies가 rank순 정렬 + 페이지 회사에 rank/score/축등수 부착. list.html 표시.
- 실사용 검증: 목록 API rank순 정렬·score 정상.

### Phase 4 — 엔드투엔드 검증 + g 과대평가 분포
- 데이터 흐름 엔드투엔드(백필→API→화면)는 Phase 2/3 실사용 API로 검증됨.
- **g 과대평가 위험 CONFIRMED** — 아래 별도 절.

## red-team 발견·수정 (각 메인이 독립 재현 판정)
- **Phase 1 [1] 치명**: 백필이 stale roic=0.0을 신뢰 → 9,251행 가짜 g=0.0. → 방향 B(None 통일)로 차단.
- **Phase 1 [2] 높음**: 마이그레이션이 5선 데이터만 바꾸고 passed_second_filter 재계산 안 함 →
  통과목록 9건 불일치. → recompute_second_filter command로 교정(불일치 0).
- **Phase 1 [3] 보통(latent)**: 마이그레이션이 라이브 함수 import. → 0022를 historical 모델 self-contained화.
- **Phase 3 [1] 보통**: price=ev/ic가 invested_capital<0에서 부호 반전 → 거짓 바겐이 가격축 최상위.
  라이브 0건이나 메커니즘 확정. → ic<=0 배제 가드 + 회귀 테스트.
- **Phase 3 [2] 보통(성능)**: 페이지마다 전수 랭킹 재계산 O(N²). 현재 151개 무해. → docstring 한계 기록.

## g 과대평가 검증 (메모리 1순위 위험지점)
실데이터 분포(g 채워진 445행 / 전체 15,382행):
- **배당 None 비율 89.9%**(g 행 기준), 전체 dividend_paid는 **99.6%가 None**(>0은 44행뿐).
- None행 전부 g==roic(유보율 b=1.0 작동 — 계산 사양은 정상). None행 전부 흑자(net_income>0).
- None행 g평균 0.169 > 배당있음 g평균 0.118 → 과대평가 방향 확인.

**판정**: calculate_g 로직은 사양대로(배당 None→유보율 1.0). 문제는 **입력(배당) 품질** —
흑자기업 99.6%가 배당 None은 비현실적이며 **dividend_paid 수집 누락**이 근본 원인. 이로 인해
거의 모든 g가 g=roic가 되어 growth 축이 quality(roic−wacc)와 사실상 중복, 랭킹 변별력이 제약됨.

이상치: |g|>1인 6행은 roic 자체가 비정상(IC 과소·자본잠식, 예 01529982 equity −379억)인 회사가
g=roic로 전파된 것. roic 데이터 품질 문제.

## 이전 메타리뷰 'Z''·Zmijewski 데드온어라이벌' 경고 반박
직전 세션 메타리뷰는 "current_assets·current_liabilities·retained_earnings를 dart_extractor가
미추출 + load 미하이드레이트 → 실데이터 상시 None, 합성픽스처로만 green"이라 적발했었다.
현물 대조(2026-07-01): current_assets 96.7%·current_liabilities 96.2%·retained_earnings 96.9%가
실제 채워져 있고, altman_z 채워진 14,551행 전부 current_assets 보유 → "상시 None" 전제는
현재 데이터와 불일치(낡음). load_company_from_db가 current_assets를 안 읽는 것은 사실이나,
**저장형 채택**(백필이 YearlyFinancialData 모델 행을 직접 접근해 계산·저장, api는 저장된
altman_z 컬럼을 읽음)이 그 미하이드레이트 함정을 구조적으로 우회한다. Z''·Zmijewski 실데이터
정상 작동 확인. (조회시 계산을 택했다면 메타리뷰대로 None이 났을 것 — 저장형 선택이 이 함정도 회피.)

## 후속 과제 (이번 범위 밖)
1. **dividend_paid 수집 경로 점검** — dart_extractor의 배당금 추출이 99.6% None을 내는 원인 규명.
   배당 수집이 정상화돼야 g가 의미를 갖고 growth 축이 quality와 독립 정보를 제공.
2. **roic 이상치 가드** — 자본잠식·IC 과소 회사의 비정상 roic이 g·랭킹에 전파되는 것 차단.
3. 정본 아젠다6(ERP 문서텍스트·ICR 기록 갱신)은 미수행.

## 데이터 현황
- 통과기업 151개 중 roic/g 데이터 보유 52개, 99개는 미수집(전 연도 roic None) → 랭킹 최하위 동순위.
  실회사 재수집으로 개선 영역.
- 테스트: 251 green(기존 226 + 5선·랭킹·방향B 신규 25).
